[project]
name = "columbia-ss"
version = "0.1.0"
description = "Bench adoption demo"
readme = "README.md"
authors = [
    { name = "Minh Le", email = "lehnminh@gmail.com" }
]
requires-python = ">=3.13"
dependencies = ["Flask>=3.1,<4"]

[project.scripts]
columbia-ss = "columbia_ss:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
